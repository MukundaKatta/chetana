{
  "name": "@chetana/probes",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@chetana/shared": "workspace:*",
    "@chetana/models": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
